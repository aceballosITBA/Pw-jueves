export default function HeroSection() {
  return (
    <section className="hero-section" aria-label="Portada principal">
      <div className="hero-content">
        <p className="hero-chip">Cerveza artesanal</p>
        <h2>Bound</h2>
        <p className="hero-lead">Todas las variedades disponibles para compra directa.</p>
        <a href="#catalogo" className="hero-cta">Ver productos</a>
      </div>
    </section>
  );
}
